# Резюме проекта: Цифровой двойник с отказоустойчивым управлением и базой данных

## Краткое описание

Проект представляет собой **отказоустойчивую систему цифрового двойника** технологического процесса управления уровнем жидкости в резервуаре. Система построена на микросервисной архитектуре с использованием Docker контейнеров, протокола OPC UA, PostgreSQL базы данных для хранения исторических данных и REST API сервиса для аналитики, отчетности и управления уставкой, обеспечивая высокую надежность через автоматическое переключение между основным и резервным контроллерами.

## Ключевые особенности

### 🛡️ Отказоустойчивость
- **Горячий резерв**: резервный контроллер работает в режиме мониторинга
- **Автоматическое переключение**: при отказе основного контроллера резервный активируется через 2 секунды
- **Автоматическое восстановление**: при возврате основного контроллера управление передается обратно
- **Heartbeat мониторинг**: контроллеры отслеживают состояние друг друга через OPC UA
- **Сохранение состояния PID**: интегральная составляющая и состояние регулятора сохраняются при переключениях
- **Плавное переключение**: нет скачков управляющего воздействия при смене контроллеров

### 🏭 Технологический процесс
- **Модель бака**: высота 3м, диаметр 2м, плотность жидкости 1000 кг/м³
- **Регулирующий клапан**: расчет по ГОСТ формулам, Kvs = 800
- **ПИД-регулирование**: настроенное по ГОСТ стандартам (Kp=15.0, Ki=0.1, Kd=1.0)
- **Целевая производительность**: стабилизация уровня на уставке 1.6м

### 🐳 Микросервисная архитектура
- **7 Docker контейнеров**: OPC UA сервер, модель процесса, основной и резервный контроллеры, watchdog, PostgreSQL база данных, сервис аналитики
- **Единый файл контроллера**: `universal_controller.py` работает в режимах primary/backup
- **Централизованная конфигурация**: все параметры в `config.json`
- **Изолированная сеть**: безопасное взаимодействие через Docker network
- **Watchdog мониторинг**: отдельный контейнер для наблюдения за состоянием контроллеров
- **PostgreSQL база данных**: надежное хранение исторических данных
- **REST API сервис**: удобный доступ к данным, аналитике и управлению уставкой

## Архитектура системы

```
┌─────────────────┐
│   OPC UA Server │ ← Центральный узел данных
│                 │
│ ┌─────────────┐ │
│ │ Variables   │ │ ← Переменные процесса + статус контроллеров
│ │ Heartbeats  │ │
│ └─────────────┘ │
└─────────────────┘
         │
    ┌────┼────┐
    │    │    │
┌───▼──┐ │ ┌──▼───┐
│Model │ │ │Ctrl-1│ ← Основной контроллер (активен)
│ Tank │ │ │(PRI) │
│Valve │ │ └──────┘
└──────┘ │
         │ ┌──────┐
         └─│Ctrl-2│ ← Резервный контроллер (мониторинг)
           │(BAK) │
           └──────┘
             │
             ▼
    ┌─────────────────┐
    │ PostgreSQL DB   │ ← База данных
    │                 │
    │ ┌─────────────┐ │
    │ │Process Data │ │ ← Данные процесса
    │ │PID States   │ │ ← Состояния контроллеров
    │ │Failover Ev. │ │ ← События переключения
    │ └─────────────┘ │
    └─────────────────┘
             │
             ▼
    ┌─────────────────┐
    │ Analytics API   │ ← REST API сервис
    │                 │
    │ ┌─────────────┐ │
    │ │REST Endpoints│ │ ← API для данных
    │ │Statistics   │ │ ← Статистика и отчеты
    │ └─────────────┘ │
    └─────────────────┘
```

## Компоненты системы

### 1. OPC UA Server
- **Функция**: Централизованное хранение данных
- **Переменные процесса**: PV_level, SP_level, OP_valve, flows
- **Переменные отказоустойчивости**: статусы контроллеров, heartbeats, active_controller
- **Переменные состояния PID**: сохранение интегральной составляющей, предыдущей ошибки и производной
- **Порт**: 4840

### 2. Process Model
- **Функция**: Симуляция физического процесса
- **Компоненты**: TankModel (материальный баланс), Valve (ГОСТ расчеты)
- **Обновление**: каждые 100мс
- **Независимость**: работает даже при отказе контроллеров

### 3. Universal Controller (Primary)
- **Функция**: Основное управление процессом
- **Режим**: Активное ПИД-регулирование
- **Heartbeat**: отправляет каждую секунду
- **Восстановление**: автоматически возвращает управление при возврате в строй

### 4. Universal Controller (Backup)
- **Функция**: Резервное управление
- **Режим**: Мониторинг основного контроллера
- **Активация**: при timeout основного (5сек) + задержка (2сек)
- **Возврат**: автоматически передает управление при восстановлении основного

### 5. PostgreSQL Database
- **Функция**: Хранение исторических данных
- **Схемы**: process_data, controller_data, system_logs, configurations
- **Таблицы**: process_variables, pid_states, failover_events, performance_metrics
- **Пользователи**: process_user (запись), readonly_user (чтение)
- **Порт**: 5432

### 6. Analytics Service
- **Функция**: REST API для аналитики, отчетности и управления уставкой
- **Framework**: Flask
- **Endpoints**: 9 основных API endpoints (включая управление уставкой)
- **Данные**: статистика процесса, история PID, события переключения, управление уставкой
- **OPC UA интеграция**: подключение к серверу для изменения уставки
- **Порт**: 8080

### 7. Watchdog
- **Функция**: Мониторинг состояния контроллеров
- **Метод**: OPC UA heartbeat мониторинг
- **Логирование**: события переключения в базу данных

## Технические характеристики

### Параметры модели
```json
{
  "tank_height": 3.0,           // м
  "tank_diameter": 2.0,         // м  
  "liquid_density": 1000.0,     // кг/м³
  "constant_inlet_flow": 100.0, // м³/ч
  "initial_liquid_level": 1.5,  // м
  "valve_kvs": 800             // максимальная пропускная способность
}
```

### Параметры ПИД-регулятора
```json
{
  "kp": 15.0,                  // пропорциональный коэффициент
  "ki": 0.1,                   // интегральный коэффициент  
  "kd": 1.0,                   // дифференциальный коэффициент
  "setpoint": 1.6,             // уставка (м)
  "integral_limit": 5.0        // ограничение интеграла
}
```

### Параметры отказоустойчивости
```json
{
  "controller_heartbeat_timeout": 5.0,  // таймаут heartbeat (сек)
  "controller_failover_delay": 2.0      // задержка переключения (сек)
}
```

### Параметры базы данных
```json
{
  "postgres_version": "15",
  "database_name": "digital_twin_db",
  "port": 5432,
  "schemas": ["process_data", "controller_data", "system_logs", "configurations"],
  "users": {
    "process_user": "полный доступ для записи",
    "readonly_user": "только чтение для аналитики"
  }
}
```

### Параметры API сервиса
```json
{
  "framework": "Flask",
  "port": 8080,
  "endpoints": [
    "/health",
    "/api/process/latest",
    "/api/process/statistics", 
    "/api/controller/pid-history",
    "/api/system/failover-events",
    "/api/system/performance",
    "/api/config/current",
    "/api/setpoint/current",
    "/api/setpoint",
    "/api/setpoint/history"
  ],
  "setpoint_management": {
    "range": "0.5 - 2.5 метров",
    "validation": "автоматическая проверка диапазона",
    "history": "логирование всех изменений",
    "opcua_integration": "прямое изменение через OPC UA"
  }
}
```

## Сценарии работы

### Нормальная работа
1. **Основной контроллер** активен, управляет процессом
2. **Резервный контроллер** мониторит, отправляет heartbeat
3. **Модель** непрерывно рассчитывает процесс
4. **OPC UA сервер** обеспечивает обмен данными
5. **Модель** сохраняет данные процесса в PostgreSQL
6. **Контроллеры** сохраняют состояния PID в PostgreSQL
7. **Analytics API** предоставляет доступ к данным через REST API

### Отказ основного контроллера
1. **Основной контроллер** перестает отправлять heartbeat
2. **Резервный контроллер** обнаруживает timeout (5 секунд)
3. **Задержка переключения** 2 секунды для избежания ложных срабатываний
4. **Резервный контроллер** активируется и берет управление
5. **Модель** продолжает работу без прерывания
6. **Резервный контроллер** сохраняет событие переключения в PostgreSQL
7. **Analytics API** может предоставить информацию о событии переключения

### Восстановление основного контроллера
1. **Основной контроллер** восстанавливается и отправляет heartbeat
2. **Резервный контроллер** обнаруживает восстановление
3. **Основной контроллер** устанавливает себя как активный
4. **Резервный контроллер** возвращается в режим мониторинга
5. **Управление** плавно передается основному контроллеру
6. **Основной контроллер** восстанавливает состояние PID из OPC UA
7. **Резервный контроллер** сохраняет событие возврата в PostgreSQL
8. **Analytics API** может предоставить информацию о событии возврата

## Команды управления

### Запуск системы
```bash
# Полный запуск
docker-compose up --build -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f
```

### Тестирование отказоустойчивости
```bash
# Остановка основного контроллера
docker-compose stop controller-primary

# Наблюдение за активацией резервного
docker-compose logs controller-backup --tail=10

# Восстановление основного
docker-compose start controller-primary

# Проверка возврата управления
docker-compose logs controller-backup --tail=10
```

### Мониторинг системы
```bash
# Логи основного контроллера
docker-compose logs controller-primary -f

# Логи резервного контроллера  
docker-compose logs controller-backup -f

# Логи модели процесса
docker-compose logs model -f

# Логи базы данных
docker-compose logs database -f

# Логи сервиса аналитики
docker-compose logs analytics -f
```

### Работа с базой данных
```bash
# Подключение к базе данных
docker exec -it digital-twin-db psql -U postgres -d digital_twin_db

# Проверка количества записей
docker exec digital-twin-db psql -U postgres -d digital_twin_db -c "
SELECT 'process_data.process_variables' as table_name, COUNT(*) as record_count
FROM process_data.process_variables
UNION ALL
SELECT 'controller_data.pid_states' as table_name, COUNT(*) as record_count
FROM controller_data.pid_states;"

# Последние записи процесса
docker exec digital-twin-db psql -U postgres -d digital_twin_db -c "
SELECT timestamp, pv_level, sp_level, op_valve, outlet_flow
FROM process_data.process_variables 
ORDER BY timestamp DESC 
LIMIT 5;"
```

### Использование API
```bash
# Проверка состояния сервиса
curl -s "http://localhost:8080/health" | python3 -m json.tool

# Последние данные процесса
curl -s "http://localhost:8080/api/process/latest?limit=5" | python3 -m json.tool

# Статистика за час
curl -s "http://localhost:8080/api/process/statistics?hours=1" | python3 -m json.tool

# История PID контроллера
curl -s "http://localhost:8080/api/controller/pid-history?limit=3" | python3 -m json.tool

# События переключения
curl -s "http://localhost:8080/api/system/failover-events?limit=5" | python3 -m json.tool

# Текущая конфигурация
curl -s "http://localhost:8080/api/config/current" | python3 -m json.tool

# Управление уставкой
curl -s "http://localhost:8080/api/setpoint/current" | python3 -m json.tool
curl -X POST "http://localhost:8080/api/setpoint" \
  -H "Content-Type: application/json" \
  -d '{"setpoint": 1.6, "reason": "Оптимизация процесса"}' | python3 -m json.tool
curl -s "http://localhost:8080/api/setpoint/history?limit=5" | python3 -m json.tool
```

### Тестирование API
```bash
# Автоматическое тестирование API уставки
./test_setpoint_api.sh
```

## Индикаторы состояния

### В логах основного контроллера
- `ОСНОВНОЙ PID: SP=1.600м, PV=1.587м, OP=45.2%` - активное управление
- `ОСНОВНОЙ (мониторинг)` - режим мониторинга (после восстановления)

### В логах резервного контроллера
- `РЕЗЕРВНЫЙ (мониторинг): SP=1.600м, PV=1.587м, OP=45.2%` - нормальный режим
- `⚠️ Основной контроллер недоступен` - обнаружение отказа
- `🚨 ПЕРЕКЛЮЧЕНИЕ НА РЕЗЕРВНЫЙ КОНТРОЛЛЕР!` - активация резервного
- `РЕЗЕРВНЫЙ ПИ расчет: SP=1.600, PV=1.587, Output=45.2` - активное управление
- `✅ Основной контроллер восстановлен` - возврат к мониторингу

## Преимущества решения

### 🔧 Техническая надежность
- **Непрерывность управления**: система никогда не остается без контроллера
- **Быстрое переключение**: активация резервного за 7 секунд (5+2)
- **Автоматическое восстановление**: возврат к основному без вмешательства оператора
- **Изоляция отказов**: отказ одного компонента не влияет на остальные
- **Надежное хранение данных**: PostgreSQL обеспечивает персистентность данных
- **Историческая аналитика**: полная история работы системы доступна через API
- **Управление уставкой**: изменение уставки через REST API с валидацией и логированием

### 🏗️ Архитектурная гибкость
- **Модульность**: каждый компонент независим и заменяем
- **Масштабируемость**: легко добавлять новые компоненты или процессы
- **Стандартизация**: использование промышленного протокола OPC UA
- **Конфигурируемость**: все параметры настраиваются через config.json

### 🚀 Эксплуатационные преимущества
- **Простота развертывания**: одна команда для запуска всей системы
- **Прозрачный мониторинг**: детальное логирование всех операций
- **Безопасность**: изолированная сеть, непривилегированные пользователи
- **Производительность**: оптимизированные алгоритмы и минимальные задержки
- **Удобная аналитика**: REST API для получения статистики и отчетов
- **Управление через API**: изменение уставки без перезапуска системы
- **Исторические данные**: полная история работы системы в базе данных
- **Автоматическое тестирование**: скрипт для проверки всех функций API

## Области применения

### 🏭 Промышленные процессы
- Управление уровнем в резервуарах и емкостях
- Системы водоподготовки и очистки
- Химические и нефтехимические процессы
- Пищевая промышленность

### 🔬 Исследования и разработка
- Тестирование алгоритмов управления
- Обучение операторов на симуляторе
- Разработка новых стратегий управления
- Валидация систем автоматизации

### 🎓 Образование
- Изучение теории автоматического управления
- Практические занятия по ПИД-регулированию
- Освоение промышленных протоколов (OPC UA)
- Изучение отказоустойчивых систем

## Возможности расширения

### Дополнительные компоненты модели
- Теплообменники
- Насосы и компрессоры  
- Реакторы
- Сепараторы

### Дополнительные алгоритмы управления
- Каскадное регулирование
- Адаптивное управление
- Нечеткая логика
- Model Predictive Control (MPC)

### Интеграция с внешними системами
- **SCADA системы**: через OPC UA протокол
- **Историческая база данных**: PostgreSQL с полной историей
- **Облачная аналитика**: REST API для интеграции с облачными сервисами
- **Системы управления предприятием (MES/ERP)**: через REST API
- **Системы мониторинга**: интеграция с Prometheus, Grafana
- **Системы отчетности**: экспорт данных через API

## Заключение

Проект демонстрирует современный подход к созданию отказоустойчивых систем промышленной автоматизации. Использование микросервисной архитектуры, стандартных протоколов, контейнеризации, PostgreSQL базы данных и REST API сервиса обеспечивает высокую надежность, масштабируемость и простоту сопровождения.

**Ключевые достижения**: 
- Создана система, которая обеспечивает непрерывное управление технологическим процессом даже при отказе основного контроллера
- Реализовано надежное хранение исторических данных в PostgreSQL
- Предоставлен удобный REST API для аналитики, отчетности и управления уставкой
- Добавлена возможность изменения уставки через API с валидацией и логированием
- Обеспечена полная прозрачность работы системы через детальное логирование

Система готова к промышленному использованию и может служить основой для более сложных проектов цифровых двойников с требованиями к высокой доступности, историческому анализу и интеграции с внешними системами.